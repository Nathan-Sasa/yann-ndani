/** @type {import('tailwindcss').Config} */

module.exports = {
    darkMode: 'dark',
    theme : {
        extend: {
            colors: {
                
            }
        }
    },
    content: [
        './src/**/*.{html,ts}',
        './public/**/*.{html,ts}',
    ],
    plugins: [],
}